# -*-Makefile-*-

# how to launch the (Portland) C99 compiler
CC99 = pgcc -c99 -DHAVE_GETOPT_H=1 -D_GNU_SOURCE=1

# we use the GNU preprocessor because Portland's does weird things
CPP99 = cpp -std=c99 -DHAVE_GETOPT_H=1 -D_GNU_SOURCE=1

# other useful (non-standard) flags
CFLAGS +=
